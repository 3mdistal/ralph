- [x] Review issue #629 requirements, relevant worker/recovery code paths, and product/escalation constraints.
- [x] Update PR resolver behavior to prevent stale no-PR cache reuse (fresh lookup support + TTL-scoped cache).
- [x] Enforce uncached issue-level canonical PR resolution immediately before recovery auto-create.
- [x] Add regression tests for stale-cache branch-hop recovery behavior and uncached conflict polling.
- [x] Run targeted tests and typecheck to validate changes.
